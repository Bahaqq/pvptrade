{
  "name": "pvptrade",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @pvptrade/web",
    "build": "npm run build --workspace @pvptrade/protocol && npm run build --workspace @pvptrade/web",
    "test": "npm run test --workspace @pvptrade/protocol && npm run test --workspace @pvptrade/web",
    "typecheck": "npm run typecheck --workspace @pvptrade/protocol && npm run typecheck --workspace @pvptrade/web",
    "check": "npm run test && npm run typecheck && npm run build",
    "program:keygen": "node scripts/generate-program-keypair.mjs",
    "deployer:keygen": "node scripts/generate-devnet-deployer.mjs",
    "devnet:status": "node scripts/check-devnet-readiness.mjs"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  },
  "devDependencies": {
    "@solana/kit": "^8.2.0"
  }
}
